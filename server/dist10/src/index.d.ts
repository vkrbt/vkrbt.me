import { BlogApplication } from './application';
import { ApplicationConfig } from '@loopback/core';
export { BlogApplication };
export declare function main(options?: ApplicationConfig): Promise<BlogApplication>;
